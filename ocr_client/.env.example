# Copy to .env and adjust as needed. The Python client reads these via
# python-dotenv if available; otherwise fall back to command-line arguments.

# Base URL of the Chandra OCR server (the FastAPI wrapper).
OCR_SERVER=http://127.0.0.1:8080
